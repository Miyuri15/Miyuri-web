# Copy to .env.local (Next.js) and fill values
# SMTP settings for Nodemailer using Gmail
# Use an App Password (generate in your Google Account -> Security -> App passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=miyurilokuhewage15@gmail.com
SMTP_PASS=uyxx rgac lbrg rjsy

# Email address that will receive contact messages
RECEIVER_EMAIL=miyurilokuhewage15@gmail.com

# Optional: set to true if using port 465 (implicit TLS)
SECURE=false

# Notes:
# - For Gmail: enable 2-Step Verification and create an App Password, then put it in SMTP_PASS.
# - Do NOT commit this file to source control. Keep it secret.
